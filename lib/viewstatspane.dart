import 'package:flutter/material.dart';

class ViewStatsPane extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('View Stats Pane'),
      ),
    );
  }
}